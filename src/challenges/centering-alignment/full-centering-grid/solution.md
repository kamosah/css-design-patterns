## Solution: Centering via CSS Grid

Let's center an element perfectly within a container using CSS Grid's alignment properties for both axes.

## Solution explanation

- **Line 6** — `place-items: center` is a shorthand for `align-items: center` and `justify-items: center`. It centers all grid children along both the block (vertical) and inline (horizontal) axes simultaneously, perfectly placing `.centroid` at the center of the container.
