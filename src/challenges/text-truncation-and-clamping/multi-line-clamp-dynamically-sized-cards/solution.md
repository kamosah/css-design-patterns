## Solution: Multi-Line Clamp in Dynamically Sized Cards

Let's clamp each card's description to three lines to maintain visual alignment across a row of dynamically sized content cards.

## Solution explanation

- **Lines 1–11** — Define the grid and card styles to ensure consistent column sizing and alignment.
- **Lines 13–18** — Apply the `-webkit-line-clamp` method to clamp text inside each `.description` block to 3 lines.
